# 피드 Worker 엔드포인트.
# 비어 있으면 더미 데이터로 동작한다(헤더에 "샘플 데이터" 표시).
#
# Worker 배포 후 아래 줄의 주석을 풀고 실제 주소를 넣으면 실데이터로 전환된다.
# 예: VITE_FEED_ENDPOINT=https://holonet-feed.<계정서브도메인>.workers.dev/api/feed
#
# 이 값은 비밀이 아니다(공개 URL). Holodex 키는 Worker secret에만 있다.

VITE_FEED_ENDPOINT=
